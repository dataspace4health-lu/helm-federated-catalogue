apiVersion: v1
kind: Service
metadata:
  name: {{ include "fc-portal.fullname" . }}
  labels:
    {{- include "fc-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8088
      protocol: TCP
      name: http
  selector:
    {{- include "fc-portal.selectorLabels" . | nindent 4 }}
